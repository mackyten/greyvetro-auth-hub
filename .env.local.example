# Local dev credentials — copy to .env.local and adjust if needed.
# .env.local is gitignored; never commit real values.
#
# Usage: cp .env.local.example .env.local

KC_DB_PASSWORD=dev-db-pass
KC_ADMIN_PASSWORD=admin
