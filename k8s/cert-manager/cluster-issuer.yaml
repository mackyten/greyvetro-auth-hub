apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: selfsigned-issuer
spec:
  selfSigned: {}
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: greyvetro-ca
  namespace: cert-manager
spec:
  isCA: true
  commonName: greyvetro-ca
  secretName: greyvetro-ca-secret
  privateKey:
    algorithm: ECDSA
    size: 256
  issuerRef:
    name: selfsigned-issuer
    kind: ClusterIssuer
    group: cert-manager.io
---
apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: greyvetro-ca-issuer
spec:
  ca:
    secretName: greyvetro-ca-secret
